import styled from "@emotion/styled";

const MenuStatus = styled.span`
  display: block;
  color: ${({ theme }) => theme.colors.grayAf};
  margin-bottom: 8px;
  font-size: 22px;
  line-height: 22px;

  margin-top: 120px;
`;

export default MenuStatus;
